# Dev environment

### Python version
Recommended to use devcontainer with VS Code

### Create `.env` file

``` shell
cp .env_exemple .env
```

### Run app
``` shell
flask --app src/app.py --debug run
```